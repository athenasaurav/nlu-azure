version: "2.0"

intents:
  - greet


responses:
  utter_greet:
  - text: default greetings


actions:
- action_fallback
- action_hello

session_config:
  session_expiration_time: 60
  carry_over_slots_to_new_session: true
